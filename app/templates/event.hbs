<div class="container">
  <h1>{{model.title}}</h1>
  <p>{{model.description}}</p>
  <p>
  <ul>
    {{#each model.attendees as |attendee|}}
    <li>{{attendee.user.name}}</li>
    {{/each}}
  </ul>
  </p>
</div>
